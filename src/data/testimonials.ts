import type { Testimonial } from "@/types";

/**
 * Only add real testimonials from real people. The Testimonials section
 * stays hidden while this array is empty.
 */
export const testimonials: Testimonial[] = [];
